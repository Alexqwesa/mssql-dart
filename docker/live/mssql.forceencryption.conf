[network]
tlscert = /var/opt/mssql/certs/mssql-live.pem
tlskey = /var/opt/mssql/certs/mssql-live.key
tlsprotocols = 1.2
# Force full-session TLS. Use with docker-compose.live.force-tls.yml /
# MSSQL_FORCE_ENCRYPTION=1 live stress suites.
forceencryption = 1
